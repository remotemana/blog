
{{!-- 
<h1>{{title}}</h1>
<p>{{body}}</p>
<h6>{{createdAt}}|{{Username}}</h6>
<hr>
<p>{{commentBody}}</p>
<form>
<button type="button" id="comment">comment</button>
<input type="text" id="commentInput" name="postId" value="{{id}}">
</form> --}}


<div>
    <h1>{{title}}</h1>
    <p>{{body}}</p>
    <h6>{{createdAt}}|{{Username}}</h6>
    <hr>
    <p>{{commentBody}}</p>

</div>
    <form>
        <input type="hidden" name="blog-id" value="{{id}}" />
        <input id="commentInput" />
        <button type="submit" id="comment">comment</button>
    </form>

<script src="/js/comment.js"></script>