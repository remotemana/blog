<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      {{user.username}} {{created_at}}
    </div>
    <div class="text">
      {{commentBody}}
    </div>
  </section>
  {{/each}}
</div>